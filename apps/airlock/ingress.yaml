apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: airlock
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    traefik.ingress.kubernetes.io/router.tls: "true"
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - airlock.agusbravo.dev
      secretName: website-tls
  rules:
    - host: airlock.agusbravo.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: airlock
                port:
                  number: 80

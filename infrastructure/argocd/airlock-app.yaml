apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: airlock
  namespace: argocd
  annotations:
    argocd.argoproj.io/refresh: hard
spec:
  project: default
  source:
    repoURL: https://github.com/agustinbravop/homelab
    targetRevision: main
    path: apps/airlock
    kustomize:
      images:
        - ghcr.io/agustinbravop/airlock:latest
  destination:
    server: https://kubernetes.default.svc
    namespace: airlock
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
